rules_version = '2';

// TEMPORARY — wide open while the app is in active development.
// MUST be locked back down to per-user/owner rules before going live.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
