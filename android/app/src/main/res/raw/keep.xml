<?xml version="1.0" encoding="utf-8"?>
<!-- The custom notification sound is referenced only at runtime (by name via
     getIdentifier), so the resource processor must be told to keep it. -->
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@raw/delivery_ring" />
